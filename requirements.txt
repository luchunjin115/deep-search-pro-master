# Direct runtime dependencies for the M1 and M2 application.
# Compatible ranges prevent accidental major upgrades. A lock file comes later.
fastapi>=0.129,<0.130
uvicorn[standard]>=0.41,<0.42
pydantic>=2.12,<3
pydantic-settings>=2.13,<3
jsonschema>=4.26,<5
SQLAlchemy>=2.0.46,<3
psycopg[binary]>=3.2,<4
alembic>=1.16,<2
PyJWT>=2.10,<3
pwdlib[argon2]>=0.3,<1
langgraph>=1.0.9,<2
langchain-openai>=1.1.10,<2
httpx>=0.28,<1
python-dotenv>=1.2,<2

# M2 file ingestion, parsing, PostgreSQL vectors, and local retrieval models.
# Declaring FlagEmbedding does not download BGE model weights.
python-multipart>=0.0.32,<0.1
PyMuPDF>=1.28,<2
python-docx>=1.2,<2
openpyxl>=3.1.5,<4
pandas>=3.0,<4
filetype>=1.2,<2
charset-normalizer>=3.4,<4
pgvector>=0.5,<1
jieba==0.42.1
FlagEmbedding>=1.4,<2

# M2-11.2 complex-document benchmark. Docling stays out of the main parser path
# until the later Router step; RapidOCR + ONNX Runtime are the fixed CPU OCR path.
docling==2.123.1
rapidocr==3.9.2
onnxruntime==1.23.2
