# 项目协作规则

本文件适用于整个仓库。任何后续开发、重构、调试和文档工作都必须遵守。

## 1. 用户背景与共同目标

- 用户是编程初学者，不得默认用户已经掌握前端、后端、数据库、异步编程、Agent或RAG。
- 本项目计划写入简历，因此“代码能够运行”和“用户真正理解代码”同等重要。
- 解释必须围绕本项目的真实调用链和业务场景，不只给出抽象概念或命令清单。
- 优先以最快速度完成有工程含金量的核心闭环，避免为了技术数量扩大范围。

### 1.1 项目级长期目标（后续会话持续生效）

- 本项目的最高目标是帮助用户在秋招中获得 AI 应用方向岗位，重点匹配 AI 应用工程师、Agent 工程师、RAG 工程师和偏 AI 后端工程师，而不是以堆砌技术名词或完成普通聊天 Demo 为目标。
- 所有范围、顺序和架构取舍都要同时回答三个问题：是否形成招聘者可运行、可观察、可量化验证的完整作品；是否保留足以区分普通聊天机器人和普通 RAG Demo 的工程含金量；用户是否能够独立向面试官解释真实调用链、设计取舍、指标、失败案例和改进过程。
- 在用户再次明确调整前，M4 通用有界深度研究和 M5 评估、加固与作品化仍是秋招主线最终目标。可以为了更早验证而调整顺序、缩小单步边界，但不得把“后移”擅自改成“删除”，也不得只完成 M1/M2 就把整个求职项目标记为完成。
- 协作过程必须帮助用户真正理解项目。每个关键能力完成后，除运行验证外，还要用本项目实例讲清“输入是什么、经过哪些层、为什么这样设计、失败会在哪里、如何定位”，并在进入下一关键能力前给用户留下提问或确认理解的机会。
- 简历、README、演示和面试材料只能描述已经实现并实际验证的能力；计划中的多 Agent、记忆、深度研究或评估不得提前包装成完成项。

## 2. 教学式协作模式

所有开发任务必须遵守以下顺序：

1. 操作前说明当前缺少什么能力。
2. 先用最通俗的大白话说明准备做什么，再补充必要的技术描述。
3. 说明为什么需要这样做，以及它解决了什么问题。
4. 专业词第一次出现时立即解释含义，并尽量使用本项目的例子。
5. 每次只完成一个便于理解和验证的小步骤，不提前扩展到后续模块。
6. 操作后列出具体修改的文件、每个文件的代码职责和实际效果。
7. 说明本次代码位于“前端 → API → Schema → Agent/LangGraph → Harness → Tool → Service → Repository/Model → PostgreSQL/pgvector/Storage/外部Provider”整条链路中的哪个位置；未经过的层也要明确说明。
8. 必须实际验证，并说明验证方法、验证结果、结果能证明什么，以及不能证明什么。
9. 每个开发步骤结束时，说明可能出现的问题，以及出现问题后优先从哪个方向排查。
10. 如果用户尚未理解当前步骤，或者明确要求先讲解，必须停止继续开发并等待用户确认。

回答不能只罗列命令、文件名或“测试通过”，必须解释这些操作之间的关系。

## 3. 阶段开始前的方案确认

每一个里程碑或阶段开始前，必须先提交该阶段的实施方案，至少包含：

- 当前现状和缺少的能力；
- 本阶段目标与明确不做的内容；
- 前置条件；
- 按顺序排列的实施步骤；
- 每个步骤预计修改或新增的文件；
- 在完整调用链中的位置；
- 每一步的验证方式；
- 阶段完成标准；
- 主要风险和排查方向。

只有用户明确确认该阶段方案后，才能开始该阶段代码开发。确认一个阶段不代表自动授权实施后续阶段。

## 4. 单步开发规则

- 一个步骤只解决一个清晰问题，并保持变更容易回退和检查。
- 开始编码前，先说明本步输入、输出和上下游关系。
- 完成编码后先验证，再汇报，不把未验证的推测描述为完成。
- 如果验证失败，先解释失败发生在哪一层，再提出修复方案。
- 不因为发现后续需求而顺手实现；记录到后续计划，等待阶段确认。

### 4.1 默认开发模式：垂直能力开发

- 后续默认按“一个可运行、可解释、可验证的小能力”划分步骤，不按内部文件、类或函数机械拆步。Schema、Runner、Report、CLI如果共同服务同一个小能力且可以一次讲清，应在同一步内完成；只有跨越独立风险或运行环境时才拆开。
- 每一步优先打通最短真实调用链，并明确本步不经过的层；不为了制造完整目录结构提前创建未来模块、接口、配置、脚本或空壳。
- 新增文件前必须先搜索仓库中是否已有相同职责、相邻能力或可扩展入口。能在现有文件中清晰完成就直接扩展；只有出现新的稳定职责时才新增生产文件。
- 保留TDD，但RED必须优先证明真实行为或合同缺口，例如权限未拦截、失败被错误记为0分、报告泄露私有字段。`ModuleNotFoundError`只能作为新能力尚不存在的起点，不能作为唯一的RED价值，也不能为了制造RED专门新建测试文件。
- 一个能力完成后再统一做相邻回归、进度记录和用户讲解；编码过程中的内部微调不升级为新的项目步骤，不重复创建过程文档或请求授权。

### 4.2 风险驱动的测试规则

- 测试文件按“稳定能力 + 运行层级”组织，不按开发步骤编号或生产文件数量一一对应。同一能力、同一层级的新测试默认追加到已有测试文件。
- 只有以下情况可以新增测试文件：出现新的稳定能力；需要隔离不同运行环境（Unit、PostgreSQL/pgvector Integration、真实模型或外部Provider Smoke、Frontend E2E）；现有文件已经职责混杂且能够清楚拆分。阶段方案必须说明每个新测试文件为什么不能复用现有文件。
- Report和CLI如果只是同一Runner的输出与入口，默认放进该Runner的测试文件；简单参数转发、常量读取、无分支包装器不单独建立测试文件。
- 每个非平凡能力至少保留一个正常路径检查，并按真实风险补充最少的失败路径。不同输入只是在验证同一规则时优先参数化，不复制多个近似测试函数。
- 不得为了减少数量删掉信任边界、tenant/ACL、版本与软删除、数据丢失保护、数据库约束与迁移、外部Provider身份、失败非数值、报告脱敏等关键验证；这些边界不同，可以分别保留Unit、Integration或Smoke证据。
- 共享Fixture或测试辅助函数只在确有重复时提取；不为单次使用创建测试框架、Factory或新的辅助文件。
- 验证分三级：开发中运行直接相关的聚焦测试；一个完整小能力结束时运行相邻回归及Ruff/Mypy/编译检查；里程碑收口或修改公共边界时再运行完整允许范围。不得在每次内部微调后机械重复全套验证。
- 每个能力收口前检查本次是否产生只有1至2个测试的碎片文件、重复Fixture或重复常量断言；能合并的在标记完成前合并。目标是用最少文件留下足以证明能力和关键风险的证据，不以测试文件数或测试用例数作为成绩。

## 5. 每一步的汇报模板

开发步骤完成后的说明应尽量包含：

1. 本步解决了什么问题；
2. 用大白话解释运行过程；
3. 修改文件与职责；
4. 完整调用链位置；
5. 验证方法与实际结果；
6. 能证明和不能证明的内容；
7. 常见问题与排查思路；
8. 是否需要用户理解或确认后再继续。

## 6. 进度记录规则

- `docs/PROJECT_PROGRESS.md` 是项目总进度看板和所有阶段记录的统一入口，只保留当前状态、里程碑摘要、关键决策、阻塞项和阶段链接。
- M1及后续每个大阶段必须使用独立目录，统一结构为 `docs/progress/M{编号}/M{编号}_{阶段名称}.md` 加同级 `records/` 子目录；阶段入口只保存当前状态、范围、有效决策、步骤索引、风险、验证基线、下一动作和过程记录链接，不得持续追加完整实施流水账。M0是已完成的早期设计记录，保留现有单文件结构。
- `records/` 保存阶段原始方案、用户确认、每个能力过程的实施日志和完整验证证据。过程文件按一个连贯能力或合理步骤范围分组，例如 `M2_13_15_INDEXING.md`，既避免重新形成单个超大文件，也避免为每个微小操作创建碎片文件。
- 尚未进入方案讨论的未来阶段不提前创建空目录或空进度文件；首次提交该阶段实施方案时，同时创建阶段目录、短入口和 `records/M{编号}_00_STAGE_PLAN.md`。后续只有出现真实过程记录时才新增对应文件。
- 每个阶段开始前，先读取总进度看板和当前阶段入口，确认已完成内容、遗留问题和下一步；开始具体任务时只按入口指引读取与当前任务及直接上游有关的 `records/` 过程记录，排查历史问题时再扩展读取，不默认全量加载所有归档。
- 每个开发小步骤完成并验证后，必须在同一次工作中把完整步骤日志写入当前阶段对应的 `records/` 文件，并只在阶段入口同步状态、步骤索引、有效决策、风险、验证基线和下一动作。如果当前步骤、阶段状态、阻塞项或里程碑发生变化，还必须同步更新总进度看板。
- 未完成实际验证的工作不得标记为“已完成”；应标记为“进行中”或“受阻”。
- 进度记录至少包含：日期、阶段、步骤、目标、修改文件、调用链位置、验证方法、实际结果、能证明的内容、不能证明的内容、风险或问题、下一步。
- 如果代码后来被修改导致原验证失效，必须在进度文件中补充说明，不能保留过时的“已验证”结论。
- `docs/design/` 保存项目级总体设计；`docs/progress/M{编号}/` 的阶段入口负责导航和当前摘要，`records/` 负责该阶段实施方案、确认记录、步骤日志、验证结果和遗留问题，避免入口、总看板和过程记录互相重复抄写。
- 阶段状态统一使用：`待确认`、`待开始`、`进行中`、`受阻`、`已完成`。

## 7. 已确认的项目边界

- 千问负责主推理和多模态理解。
- Agent编排使用LangGraph。
- 业务数据库使用PostgreSQL。
- 向量存储使用pgvector。
- Embedding使用BGE-M3，Reranker使用BGE-Reranker。
- RAG由项目自行实现，不依赖RAGFlow。
- 互联网搜索使用`search_public_web`与`read_public_source`两个受控Tool，经Service和可替换Provider调用Tavily；模型不能提交任意URL或HTTP请求。
- V1使用本地文件存储并封装Storage接口，不部署MinIO。
- 数据使用明确标注的合成演示数据。
- M1库存查询已完成；当前先完成M2自建RAG与多Agent闭环，M3多模态退出秋招主线并暂缓，随后完成M4通用有界深度研究和M5评估作品化。
- Tool数量不是目标；只实现真实闭环需要的单一职责Tool。每个Agent按角色和任务只获得1至5个允许工具，不把全部工具同时交给模型。
- 复杂、可复用、具有固定业务步骤的能力才封装为业务Skill；当前秋招主线只确认M4实现一个`cross-border-market-research` Skill。简单查询保留为Tool，文件转换、固定分析和报告格式化等确定性逻辑保留为Service。
- Harness作为运行时护栏逐阶段实现，统一负责上下文、权限、工具白名单、预算、超时重试、审计、证据和评估钩子。
- V1不引入MCP；内部能力先直接使用Python接口，未来连接真实Amazon SP-API、ERP或向外部客户端开放能力时再评估MCP。
