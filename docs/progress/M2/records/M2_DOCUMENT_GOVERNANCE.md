# M2 文档治理记录

> 当前状态和阅读入口见 [M2入口](../M2_KNOWLEDGE_RAG.md)。

## 2026-09-01｜拆分M2入口与过程记录

**状态：已完成并验证。**

### 1. 本步解决的问题

迁移前的M2单文件同时保存当前状态、总体方案、M2-01至M2-18实施日志和全部验证流水账，达到约3710行、508090字节。新窗口为了确认当前停止点，需要加载大量与当前任务无关的历史内容。

### 2. 大白话运行过程

把原来的一本“大总账”改成“目录页加分类账”：M2入口只告诉Agent现在做到哪里、哪些决定仍有效、下一步应该读什么；原始方案和每个能力阶段的完整日志分类放入`records/`，需要修改或排查对应能力时才读取。

### 3. 修改文件与职责

- `docs/progress/M2/M2_KNOWLEDGE_RAG.md`：新的M2唯一入口，保存当前状态、阅读顺序、记录索引、步骤状态、关键决策、风险和下一动作；
- `docs/progress/M2/records/M2_00_STAGE_PLAN.md`：保存M2原始阶段方案；
- `M2_01_06_FOUNDATION.md`至`M2_18_CONTEXT_EVIDENCE.md`：按能力保存原始完整方案、实施日志与验证证据；
- `docs/PROJECT_PROGRESS.md`：删除重复的逐步流水账，保留当前状态、跨项目决定、验证基线、风险和阶段链接；
- `AGENTS.md`：允许大型阶段使用“阶段入口 + records过程记录”，并规定新窗口按当前任务读取，不默认全量加载归档；
- 迁移前的M2单文件：迁移后删除，由新目录入口替代。

### 4. 调用链位置

本步只属于项目文档与协作治理层，没有经过“前端 → API → Schema → Service → Model → PostgreSQL”中的任何运行层，也没有修改应用、迁移、测试、Seed、Storage或数据库。

### 5. 验证方法与实际结果

- 使用Git中的迁移前文件作为只读基线，按八个确定性边界重新提取原始正文；
- 八份过程记录去掉新增页眉后，与原文件对应区间逐段比较，结果全部为`True`；
- M2-01至M2-18标识在过程记录中全部存在；
- 新入口、八份历史记录和总看板中的本地Markdown链接全部存在；
- 仓库中不存在仍指向旧活动路径的引用；
- `git diff --check`通过，只有Git提示未来可能把LF转换为Windows CRLF，没有空白错误；
- M2必读入口从508090字节降低到12201字节，减少97.6%；总进度看板从53576字节降低到5082字节，减少90.5%。

### 6. 能证明和不能证明

能证明：迁移前第50行开始的阶段方案与M2-01至M2-18历史正文均已进入过程记录；当前状态和下一动作有短入口；所有当前本地链接可解析；本步没有修改运行代码。

不能证明：不同模型一定节省相同数量的Token；未来Agent一定遵守按需读取；历史记录中的旧验证在代码继续变化后仍永久有效。后续仍需依靠`AGENTS.md`读取规则和每步同步更新来维持结构。

### 7. 风险、排查和下一步

- 新窗口找不到M2记录：先检查`docs/PROJECT_PROGRESS.md`是否链接到`progress/M2/M2_KNOWLEDGE_RAG.md`；
- 修改某能力时上下文不足：按入口第3节读取对应过程记录和直接上游记录，不要立即加载全部归档；
- 当前状态与历史日志冲突：以代码、迁移和实际验证为准，先修对应过程记录，再同步入口和总看板；
- 新步骤日志再次堆进入口：完整日志应写入`records/`，入口只更新一行状态、有效决定、风险和下一动作。

本步完成后仍停止在M2-18，不开始M2-19方案或代码。

## 2026-09-11｜再次压缩活动入口并拆分M2-22巨型记录

**状态：已完成并验证。**

### 1. 本步解决的问题

项目总看板增长到482行，M2入口增长到674行，M2-22单一记录增长到3829行。三个活动文件都在重复保存逐步测试数字、RED/GREEN过程和完成摘要，已经偏离“总看板 → 阶段入口 → 能力记录”的分层规则。

### 2. 大白话运行过程

先把迁移前的两个活动入口完整封存，再把M2-22按连续章节拆成原始方案、数据与Parser、Chunk、Retrieval、Reranker/Context、Answer/Citation六份能力记录。逐行确认原文能够完整拼回后，才把三个活动文件改成短入口。

### 3. 修改文件与职责

- `docs/PROJECT_PROGRESS.md`：只保留项目状态、里程碑、跨项目决定、当前验证摘要、活动风险和入口；
- `docs/progress/M2/M2_KNOWLEDGE_RAG.md`：只保留M2状态、能力地图、步骤索引、有效决定、活动风险、当前基线和下一步；
- `M2_22_CROSS_BORDER_RAG_EVALUATION_PLAN.md`：保留原路径并改为M2-22导航页；
- `M2_22_00_STAGE_PLAN.md`至`M2_22_08_ANSWER_CITATION.md`：按完整能力保存原3829行方案和实施记录；
- `M2_DOCUMENTATION_MIGRATION_SNAPSHOT_2026-09-11.md`：冻结迁移前的总看板和M2入口原文，避免未提交内容依赖Git历史；
- `M2_07_11_DOCUMENT_PARSING.md`：把四个活动链接改到新的数据与Parser记录。

### 4. 调用链位置

本步只属于文档与协作治理层，没有经过或修改“前端 → API → Schema → Agent/LangGraph → Harness → Tool → Service → Repository/Model → PostgreSQL/pgvector/Storage → 外部Provider”。

### 5. 数据保护与验证口径

- 迁移前先记录三个源文件的SHA-256和行数；
- M2-22按`1-588 / 589-1494 / 1495-2467 / 2468-2814 / 2815-3466 / 3467-3829`六个连续区间拆分；
- 六个区间合计3829行，并按原顺序逐行比较；
- 两个活动入口的迁移前全文存入单一冻结快照；
- 后续检查所有活动Markdown链接、旧M2-22文件引用、当前状态、下一动作和`git diff --check`。

实际复核结果：

- 冻结快照中的项目总看板为482行、M2入口为674行，与迁移前行数一致；
- 六份M2-22记录去掉新增导航页眉后合计3829行，第1至53节连续且各出现一次；
- 按原顺序重建正文后的规范化SHA-256为`524279b9b1237552aa0ee1e6475881f44403d39e51881a1057f31f4b39e93ead`；
- 本次活动文档的本地Markdown文件路径和章节锚点全部可解析，损坏链接为0；
- 冻结快照之外，仍指向旧M2-22文件章节锚点的引用为0；
- `git diff --check`通过；输出只有仓库换行符策略的LF/CRLF提示，没有空白错误；
- 活动文件缩减结果：项目总看板482→83行，M2入口674→186行，M2-22活动入口3829→69行。

### 6. 能证明与不能证明

能证明：迁移前的文档正文仍可追溯，M2-22的章节顺序和验证数据没有因拆分删除，活动入口不再承担逐日流水账。

不能证明：历史测试在当前或未来代码状态下仍然有效；文档链接检查也不能代替业务代码、数据库或真实Provider测试。本步没有重新运行这些业务验证。

### 7. 后续维护规则

- 一个事实只在对应能力记录保存完整版本，其他文件写摘要并链接；
- 同一完整能力继续写入同一文件，不按内部模块或微步骤新增碎片；
- 能力完成后冻结记录，后续变化必须明确旧结论是否失效；
- 总看板不复制阶段测试日志，阶段入口不倒序追加“最近完成”长列表；
- 本次迁移完成后仍停在M2-22.8.2，不开始M2-22.8.3。
