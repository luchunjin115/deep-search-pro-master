# M1文档治理记录

> 当前状态和阅读入口见 [M1入口](../M1_INVENTORY_QUERY.md)。

## 2026-09-01｜拆分M1入口与过程记录

**状态：已完成并验证。**

### 1. 本步解决的问题

迁移前的M1单文件同时保存阶段入口、原始方案、M1-01至M1-21实施日志和全部验证流水账，共3512行、262640字节。M1已经完成，新窗口通常只需要知道最终合同或读取一段直接相关历史，没有必要默认加载全部内容。

### 2. 大白话运行过程

把M1“大总账”改成“结果目录页加分类账”。入口只保留最终状态、调用链、步骤索引、关键决定和验证基线；详细方案与实施证据按基础设施、库存核心、安全Tool、模型流程、API前端和最终验收分类保存。

### 3. 修改文件与职责

- `docs/progress/M1/M1_INVENTORY_QUERY.md`：M1唯一入口；
- `records/M1_00_STAGE_PLAN.md`：M1原始方案和21步计划；
- `records/M1_01_06_FOUNDATION.md`：配置、PostgreSQL、事务和基础模型；
- `records/M1_07_11_INVENTORY_CORE.md`：Seed、Schema、Repository、业务Service和数据库Evidence；
- `records/M1_12_15_SECURITY_HARNESS_TOOLS.md`：认证、权限、Harness、Trace和Tool；
- `records/M1_16_17_MODEL_AND_GRAPH.md`：Mock/Qwen Provider和LangGraph；
- `records/M1_18_19_API_AND_FRONTEND.md`：FastAPI和Next.js；
- `records/M1_20_21_ACCEPTANCE.md`：故障矩阵、浏览器回归、公开演示和空卷复现；
- `docs/PROJECT_PROGRESS.md`：更新M1入口链接和最近完成摘要；
- `AGENTS.md`：把单文件路径示例改为仍保持单文件结构的M0，避免继续引用M1旧路径；
- 迁移前的M1单文件：拆分完成后删除。

### 4. 调用链位置

本步只属于文档和协作治理层，没有经过“前端 → API → Schema → Service → Model → PostgreSQL”任何运行层，也没有修改应用代码、迁移、测试、Seed、Docker卷或数据库。

### 5. 验证方法与实际结果

- 在删除旧入口前，以旧文件为基线按七个唯一标题边界拆分；
- 七份过程记录去掉新增页眉后，与原文件对应区间逐段比较，全部为`True`；
- M1-01至M1-21在过程记录中全部存在；
- 新入口、七份历史记录、治理记录和总看板中的本地Markdown链接全部存在；
- 仓库内不存在仍指向M1旧活动路径的引用；
- `git diff --check`通过；Windows可能把LF转换为CRLF的提示不是空白错误；
- M1必读入口从262640字节降低到11136字节、从3512行降低到168行，体积减少约95.8%。

### 6. 能证明和不能证明

能证明：迁移前从原始方案开始到M1-21收口的正文都进入了过程记录；M1-01至M1-21有记录覆盖；当前仓库链接有效；本步没有修改运行代码。

不能证明：不同模型会节省完全相同的Token；M1完成时的测试结果在后续代码版本中自动继续有效；仓库外部保存的旧文件链接已经同步更新。

### 7. 风险、排查和下一步

- 找不到M1记录：从`docs/PROJECT_PROGRESS.md`进入M1入口，不要使用迁移前路径；
- 修改库存能力时上下文不足：沿入口的记录表读取对应能力和直接上游，不默认读取全部历史；
- M1旧验证与当前M2代码冲突：实际运行当前回归，以当前代码结果为准，并注明旧结论失效范围；
- 新日志再次堆进入口：完整验证流水写入对应`records/`文件，入口只更新有效摘要。

本步不改变M1“已完成”状态，也不改变当前项目停止在M2-18、等待M2-19方案讨论的状态。

## 2026-09-01｜冻结M1及后续大阶段目录规范

**状态：已完成并验证。**

用户确认M1、M2、M3及后续每个大阶段都必须采用相同结构，不再根据内容多少选择阶段单文件。`AGENTS.md`现已规定：M1及以后统一使用`docs/progress/M{编号}/`，阶段入口与`records/`同级；首次提交阶段方案时才创建目录、入口和`M{编号}_00_STAGE_PLAN.md`，详细日志按连贯能力或合理步骤范围分组保存。M0作为已完成的早期设计记录保持单文件，不做无收益迁移。

本次只修改协作规则、总看板关键决策和本治理记录，没有修改运行代码。验证包括规则关键词、M1/M2现有目录形状、总看板链接以及`git diff --check`；能证明仓库内规范已经明确，不能证明未来执行者一定遵守，因此每个新阶段方案评审时仍需核对目录是否按规则创建。
