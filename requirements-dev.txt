-r requirements.txt

# Development, linting, and test dependencies only.
pytest>=8.4,<10
pytest-asyncio>=1.2,<2
pytest-cov>=6,<8
ruff>=0.12,<1
mypy>=1.17,<2
packaging>=25,<27
reportlab>=4.4,<5

# M2-22.6 retrieval-only semantic evaluation. Keep the exact API/version audited.
ragas==0.4.3
# Ragas 0.4.3 still imports chat_models.vertexai, removed in 0.4.2.
langchain-community==0.4.1
